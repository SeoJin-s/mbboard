<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mbboard.mapper.LoginMapper">

	<!-- 로그인 -->
	<select id="login" parameterType="com.example.mbboard.dto.Member"
			resultType="com.example.mbboard.dto.Member">
		SELECT 
			member_id AS memberId,
			member_role AS memberRole
		FROM member
		WHERE member_id = #{memberId}
		AND member_pw = #{memberPw}
	</select>

	<!-- 아이디 중복 확인 -->
	<select id="selectId" parameterType="string" resultType="string">
		SELECT member_id
		FROM member
		WHERE member_id = #{memberId}
	</select>

	<!-- 전체 회원 목록 -->
	<select id="selectMember" resultType="com.example.mbboard.dto.Member">
		SELECT member_id AS memberId, member_pw AS memberPw, member_role AS memberRole
		FROM member
	</select>

	<!-- 회원 1명 상세 조회 -->
<select id="selectMemberOne" parameterType="string" resultType="com.example.mbboard.dto.Member">
	SELECT 
		member_id AS memberId,
		member_pw AS memberPw,
		member_role AS memberRole,
		email,
		pwcktime
	FROM member
	WHERE member_id = #{memberId}
</select>


	<!-- 회원가입 -->
	<insert id="insert" parameterType="com.example.mbboard.dto.Member">
		INSERT INTO member (
			member_id,
			member_pw
		) VALUES (
			#{memberId},
			#{memberPw}
		)
	</insert>

<!-- 기존 회원 정보 수정 -->
<update id="updateMember" parameterType="com.example.mbboard.dto.Member">
	UPDATE member
	SET member_pw = #{memberPw},
	    member_role = #{memberRole}
	WHERE member_id = #{memberId}
</update>

<!-- 비밀번호 재설정용 수정 -->
<update id="updateMemberPwAfterMail" parameterType="com.example.mbboard.dto.Member">
	UPDATE member
	SET member_pw = #{memberPw},
	    pwcktime = NULL
	WHERE member_id = #{memberId}
</update>
<update id="updateMemberPwByAdmin" parameterType="com.example.mbboard.dto.Member">
    UPDATE member
    SET member_pw = #{memberPw},
        pwcktime = NOW()
    WHERE member_id = #{memberId}
</update>
</mapper>
